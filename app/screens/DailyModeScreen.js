import React from 'react'
import { Text, View } from 'react-native'

import { styles } from '../styles/styles.js'

export default function DailyMode() {
	return (
		<>
			<View style={styles.center}>
				<Text style={styles.title}>DailyModeScreen</Text>
			</View>
		</>
	)
}
